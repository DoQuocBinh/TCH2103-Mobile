import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mySecond2App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
